{% extends 'base.html.twig' %}

{% block title %}Liste des categories{% endblock %}

{% block body %}
    <div class="row">
    {% for laCategorie in lesCategories %}
        <div class="col-sm-12 col-md-12 col-lg-4 col-xl-3 mx-auto">
            <div class="card mb-3" style= "width: 18rem;">
                <h4 class="card-title text-center">
                {{laCategorie.libelle}}
                </h4>
                <img style="display: block;" src="{{laCategorie.image}}">
                <ul class="list-group list-group-flush">
                    <li class="list-group-item">{{laCategorie.description}}</li>
                </ul>
                <div class="card-body">
                    <a href="{{path('categorie',{'id': laCategorie.id})}}" class="btn btn-success btn-block text-center w-100">Détail. . .</a>
                </div>   
            </div>
        </div>
    {% endfor %}
    </div>
{% endblock %}
